import React from "react";

const SearchBox = () => {
  return (
    <div className="pa2">
      <input type="search" className="" placeholder="Search Plants" />
    </div>
  );
};

export default SearchBox;
